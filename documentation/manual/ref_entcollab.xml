<?xml version="1.0" encoding="ISO-8859-1"?>

<!--

Description of all the artifacts associated with Collaboration Diagrams within
Argo/UML.


Modifications
=============

 5 Dec 2001:  Jeremy Bennett. Created as a new chapter.

14 Feb 2002:  Jeremy Bennett. Content of chapter written.

-->

<chapter id="ch.ref.entcollab">
  <title>&argouml; Collaboration Diagram Artifact Reference</title>

  <sect1>
    <title>Introduction</title>

    <para>This chapter describes each artifact that can be created within a
      collaboration diagram. Note that some sub-artifacts of artifacts on the
      diagram may not actually themselves appear on the diagram.</para>

    <para>There is a close relationship between this material and the
      properties tab of the details pane (see
      <xref linkend="s1.ref.proptab"/>). That section covers Properties in
      general, in this chapter they are linked to specific artifacts.</para>

    <caution><para>Collaboration diagrams are not fully developed yet in
      &argouml;. Many aspects are not fully implemented, or may not behave as
      expected. In particular there are some serious problems with layout of
      the collaboration roles and messages.</para></caution>

    <!-- A diagram to show all things visible on the diagram -->

    <para><xref linkend="fig.ref.collaboration_diagram"/> shows a collaboration
      diagram with all possible artifacts displayed.</para>

    <!-- This picture should more or less correspond to the sequence diagram
         picture. -->

    <figure id="fig.ref.collaboration_diagram" float="1">
      <title>Possible artifacts on a collaboration diagram.</title>

      <screenshot><mediaobject>
        <imageobject>
          <imagedata format="GIF" align="center"
                     fileref="images/reference/collaboration_diagram.gif"/>
        </imageobject>

        <textobject>
          <para>A Collaboration Diagram, showing (from left to right and top to
            bottom) a Classifier Role, Association Role, Message and Classifier
            Role.</para>
        </textobject>
      </mediaobject></screenshot>
    </figure>
  </sect1>

  <!-- Artifacts specific to Collaboration Diagrams. Classifier Role artifact -->

  <sect1>
    <title>Classifier Role</title>

    <para>A classifier role is a specialization of a classifier, used to show
      its behavior in a particular context. In the UML metamodel
      <literal>Classifier Role</literal> is a sub-class of
      <literal>Classifier</literal>. Within a collaboration diagram classifier
      roles may be used in one of two ways:</para>

    <itemizedlist>
      <listitem><para>To represent the classifier in a particular
        behavioral context (the <emphasis>specification level</emphasis>);
        or</para></listitem>

      <listitem><para>to specify a particular instance of the classifier (the
        <emphasis>instance level</emphasis>).</para></listitem>
    </itemizedlist>

    <para>In this latter form, classifier roles are identical to the instances
      used in sequence diagrams (see <xref linkend="ch.ref.entseq"/>) and a
      collaboration diagram shows the same information as the sequence diagram,
      but in a different presentation.</para>

    <caution><para>A collaboration diagram should not mix classifier roles used
      as the specifier level and the instance level.</para></caution>

    <para>A classifier role is represented on a sequence diagram in &argouml;
      as a plain box labeled with the classifier role name (if any) and
      classifier, separated by a colon (:).</para>

    <caution><para>A classifier role should properly also show object name (if
      any) preceding the classifier role name and separated from it by a slash
      (<literal>/</literal>). This allows classifier roles in a specification
      level diagram to be distinguished from instances in an instance level
      diagram.</para>

    <para>This distinction is not implemented in the current release of
      &argouml;.</para></caution>

    <!-- Classifier Role details tabs -->

    <sect2>
      <title>Classifier Role Details Tabs</title>

      <para>The details tabs that are active for classifier roles are described
        in <xref linkend="tab.ref.classifier_role_details_tabs"/>.</para>

      <table id="tab.ref.classifier_role_details_tabs" frame="all" colsep="1"
             rowsep="1" pgwide="1">
        <title>Details tabs for classifier role</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Tab</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for ToDoItem tab -->

            <row>
              <entry>ToDoItem</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Properties tab -->

            <row>
              <entry>Properties</entry>

              <entry><para>See
                <xref linkend="tab.ref.classifier_role_property_toolbar"/> and
                <xref linkend="tab.ref.classifier_role_property_fields"/>
                below.</para></entry>
            </row>

            <!-- Entry for Documentation tab -->

            <row>
              <entry>Documentation</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Style tab -->

            <row>
              <entry>Style</entry>

              <entry><para>Standard tab.</para>

              <warning><para>In the current release of &argouml; changing the
                values of the <literal>Bounds</literal> field leads to erratic
                behavior. The <literal>Shadow</literal> field has no
                effect.</para></warning></entry>
            </row>

            <!-- Entry for Source tab -->

            <row>
              <entry>Source</entry>

              <entry><para>Standard tab, but with no contents.</para>

                <caution><para>A classifier role should not generate any code,
                  so having this tab active is probably a
                  mistake.</para></caution></entry>
            </row>

            <!-- Entry for Constraints tab -->

            <row>
              <entry>Constraints</entry>

              <entry><para>Standard tab. There are no standard constraints
                defined for <literal>ClassifierRole</literal> within the UML
                metamodel.</para>

              <note><para>This is an important distinction from
                <literal>Instance</literal> used in sequence diagrams, which is
                able to talk about creation and destruction. Where a
                collaboration is being used at instance level, it may be
                appropriate to create these constraints for use on
                collaboration diagrams.</para></note></entry>
            </row>

            <!-- Entry for Tagged Values tab -->

            <row>
              <entry>Tagged Values</entry>

              <entry><para>Standard tab. In the UML metamodel,
                <literal>Classifier Role</literal> has the following standard
                tagged values defined.</para>

                <itemizedlist>
                  <listitem><para><literal>persistence</literal> (from the
                    superclass, <literal>Classifier</literal>. Showing the
                    permanence of the state information associated with the
                    classifier role. Values <literal>transitory</literal>
                    (state is destroyed when the classifier role is destroyed)
                    and <literal>persistent</literal> (state is preserved when
                    the classifier role is destroyed).</para></listitem>

                  <listitem><para><literal>semantics</literal> (from the
                    superclass, <literal>Classifier</literal>). The value is a
                    specification of the semantics of the
                    classifier role.</para></listitem>

                  <listitem><para><literal>derived</literal> (from the
                    superclass, <literal>ModelElement</literal>). Values
                    <literal>true</literal>, meaning the classifier role is
                    redundant&mdash;it can be formally derived from other
                    elements, or <literal>false</literal> meaning it
                    cannot.</para>

                  <note><para>Derived classifier roles still have their value
                    in analysis and design to introduce useful names or
                    concepts, and in design to avoid
                    re-computation.</para></note></listitem>
                </itemizedlist>

                <note><para>The UML <literal>Element</literal> metaclass from
                  which all other artifacts are derived includes the tagged
                  element <literal>documentation</literal> which is handled by
                  the <emphasis>documentation tab</emphasis> under
                  &argouml;</para></note></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>

    <!-- Classifier Role properties -->

    <sect2>
      <title>Classifier Role Properties</title>

      <!-- The properties tool bar -->

      <para>The property tab tool bar for classifier roles is described in
        <xref linkend="tab.ref.classifier_role_property_toolbar"/>.</para>

      <table id="tab.ref.classifier_role_property_toolbar" frame="all"
             colsep="1" rowsep="1" pgwide="1">
        <title>Property tool bar for classifier role</title>

        <tgroup cols='3' colsep='0' rowsep='0'>
          <colspec align="left"   colwidth="*2"/>
          <colspec align="center" colwidth="*1"/>
          <colspec align="left"   colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Button</entry>
              <entry align="center">Icon</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Go up button -->

            <row>
              <entry>Go up</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_up.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate up through the package
                structure.</para></entry>
            </row>

            <!-- Entry for Go back button -->

            <row>
              <entry>Go back</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_back.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate back to the previous
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Go forward button -->

            <row>
              <entry>Go forward</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_forward.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate forward to the next
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Delete button -->

            <row>
              <entry>Delete</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/property_delete.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>This deletes the classifier role from the
              model</para>

              <warning><para>This is a deletion from the model
                <emphasis>not</emphasis> just the diagram. To delete an
                classifier role from the diagram, but keep it within the model, use
                the main menu <literal>Edit/Remove From Diagram</literal>
                (Ctrl-R).</para></warning></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
  
      <!-- Fields in the properties tab -->

      <para>The fields on the property tab for classifier roles are described
        in <xref linkend="tab.ref.classifier_role_property_fields"/>.</para>

      <table id="tab.ref.classifier_role_property_fields" frame="all"
             colsep="1" rowsep="1" pgwide="1">
        <title>Property fields for classifier role</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Field</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Name -->

            <row>
              <entry>Name</entry>

              <entry><para>Text box. The name of the classifier role. By
                convention classifier role names start with a lower case letter
                and use bumpy caps to divide words within the name.</para>

              <note><para>&argouml; does not enforce this naming
                convention.</para></note></entry>
            </row>

            <!-- Entry for Classifier -->

            <row>
              <entry>Classifier</entry>

              <entry><para>Drop down selector. The name of the classifier
                of which this is a classifier role.</para>

              <note><para>In the current release of &argouml; the same
                graphical presentation is used, even if the classifier role is
                actually representing an instance of an actor (when a stick-man
                would be more usual).</para></note></entry>
            </row>

            <!-- Entry for Stereotype -->

            <row>
              <entry>Stereotype</entry>

              <entry><para>Drop down selector. Classifier Role should be
                provided by default with the UML standard stereotypes for
                <literal>Classifier</literal> (<literal>metaclass</literal>,
                <literal>powerType</literal>, <literal>process</literal>,
                <literal>thread</literal> and <literal>utility</literal>).
                &argouml; also provides the additional stereotypes,
                <literal>machine</literal>, <literal>organization</literal> and
                <literal>person</literal>.</para>

              <caution><para>&argouml; also provides the stereotype
                <literal>realize</literal> for classifier roles. This appears
                to be an error, since this stereotype properly belongs to the
                <literal>Abstraction</literal>
                metaclass.</para></caution>

              <warning><para>In the current release of &argouml; the standard
                stereotypes for <literal>Classifier</literal>
                (<literal>metaclass</literal>, <literal>powerType</literal>,
                <literal>process</literal>, <literal>thread</literal> and
                <literal>utility</literal>) are not
                provided.</para></warning></entry>
            </row>

            <!-- Entry for NavStereo -->

            <row>
              <entry>Navigate Stereotype</entry>

              <entry><para><guiicon><inlinemediaobject><imageobject><imagedata
                  format="GIF" align="center"
                  fileref="images/icons/navigate_stereotype.gif"/>
                </imageobject></inlinemediaobject></guiicon> icon. If a
                  stereotype has been selected, this will navigate you to the
                  stereotype property panel (see <xref
                    linkend="s1.ref.stereotype_class"/>).</para></entry>
            </row>

            <!-- Entry for Namespace -->

            <row>
              <entry>Namespace</entry>

              <entry><para>Text box. Records the namespace for the
                classifier role. This is the package hierarchy.</para>

              <para>&Button1; click on the entry will take you to the package
                defining this namespace (or the model for the top level
                namespace).</para></entry>
            </row>

            <!-- Entry for Association Roles -->

            <row>
              <entry>Association Roles</entry>

              <entry><para>Text area. Lists the association roles that are
                linked to this classifier role.</para>

              <para>&Button1; click takes you to the selected entry, &button2;
                gives a pop up menu with five entries.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected association role.</para></listitem>

                <listitem><para><literal>Add</literal>. Add a new
                  association role.</para>

                <warning><para>In the current release of &argouml; this creates
                  an association, not an association
                  role. In any case there is no mechanism to add a newly
                  created artifact to the diagram.</para></warning></listitem>

                <listitem><para><literal>Delete</literal>. Delete the selected
                  association role.</para></listitem>

                <listitem><para><literal>Move Up</literal>. Only available
                  where there are two or more association roles, and the
                  association role selected is not at the top. It is moved
                  one up the list.</para>

                <caution><para>In the current release of &argouml; this entry
                  is always grayed out. Association roles can not be moved in
                  the order. To change the order a association role must be
                  deleted and then recreated on the
                  diagram.</para></caution></listitem>

                <listitem><para><literal>Move Down</literal>. Only available
                  where there are two or more association roles listed, and
                  the association role selected is not at the bottom. It is
                  moved one down the list.</para>

                <caution><para>In the current release of &argouml; this entry
                  is always grayed out. Association roles can not be moved in
                  the order. To change the order a collaboration role must be
                  deleted and then recreated on the
                  diagram.</para></caution></listitem>
              </itemizedlist></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>
  </sect1>

  <!-- Association Role artifact. Has Associate Role End and Message as
       sub-artifacts. -->

  <sect1>
    <title>Association Role</title>

    <para>An association role is a specialization of an association, used to
      describe an associations behavior in a particular context. In the UML
      metamodel <literal>Association Role</literal> is a sub-class of
      <literal>Association</literal>.</para>

    <para>An association role is represented on a sequence diagram in &argouml;
      as a line connecting the instances concerned. However on a sequence
      diagram the representation is modified to reflect the type of action
      associated with the stimulus carried on the link (see <xref
      linkend="s1.ref.stimulus"/>).</para>

    <para>The association role is labeled with the association role name (if
      any).</para>

    <caution><para>An association role should properly show link name (if any)
      preceding the association role name and separated from it by a slash
      (<literal>/</literal>) and be followed by the association name and
      separated from it by a colon (<literal>:</literal>) in the same manner as
      a classifier role. This allows association roles in a specification
      level diagram to be distinguished from links in an instance level
      diagram.</para>

    <para>This distinction is not implemented in the current release of
      &argouml;.</para></caution>

    <caution><para>Association roles should show the same navigability,
      aggregation and multiplicity features as their parent
      association. However these features are no implemented in the current
      release of &argouml;.</para></caution>

    <!-- Association Role details tabs -->

    <sect2>
      <title>Association Role Details Tabs</title>

      <para>The details tabs that are active for association roles are
        described in
        <xref linkend="tab.ref.association_role_details_tabs"/>.</para>

      <table id="tab.ref.association_role_details_tabs" frame="all" colsep="1"
             rowsep="1" pgwide="1">
        <title>Details tabs for association role</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Tab</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for ToDoItem tab -->

            <row>
              <entry>ToDoItem</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Properties tab -->

            <row>
              <entry>Properties</entry>

              <entry><para>See <xref
                  linkend="tab.ref.association_role_property_toolbar"/> and <xref
                  linkend="tab.ref.association_role_property_fields"/>
                below.</para></entry>
            </row>

            <!-- Entry for Documentation tab -->

            <row>
              <entry>Documentation</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Style tab -->

            <row>
              <entry>Style</entry>

              <entry><para>Standard tab. The values for the bounds of the
                association role notionally define the bounding box of the
                association role. However if you change them it will have no
                effect.</para></entry>
            </row>

            <!-- Entry for Source tab -->

            <row>
              <entry>Source</entry>

              <entry><para>Standard tab, but with no contents.</para>

              <caution><para>An association role should not generate any code,
                so having this tab active is probably a
                mistake.</para></caution></entry>
            </row>

            <!-- Entry for Constraints tab -->

            <row>
              <entry>Constraints</entry>

              <entry><para>Standard tab. In the UML metamodel,
                <literal>AssociationEnd</literal> has the following standard
                constraints defined.</para>

                <itemizedlist>
                  <listitem><para><literal>xor</literal> (from the superclass
                    <literal>Association</literal>). Applied to a set of
                    association roles, indicating that over that set, exactly
                    one association role is manifest for any particular
                    association instance.</para></listitem>
                    </itemizedlist></entry>
            </row>

            <!-- Entry for Tagged Values tab -->

            <row>
              <entry>Tagged Values</entry>

              <entry><para>Standard tab. In the UML metamodel,
                <literal>AssociationRole</literal> has the following standard
                tagged values defined.</para>

                <itemizedlist>
                  <listitem><para><literal>persistence</literal> (from the
                    superclass, <literal>Association</literal>). Values
                    <literal>transitory</literal>, indicating state is
                    destroyed when an instance is destroyed or
                    <literal>persistent</literal>, marking state is preserved
                    when an instance is destroyed.</para></listitem>

                  <listitem><para><literal>derived</literal> (from the
                    superclass, <literal>ModelElement</literal>). Values
                    <literal>true</literal>, meaning the association is
                    redundant&mdash;it can be formally derived from other
                    elements, or <literal>false</literal> meaning it
                    cannot.</para>

                  <note><para>Derived association roles still have their value
                    in analysis to introduce useful names or concepts, and in
                    design to avoid re-computation.</para></note></listitem>
                </itemizedlist>

                <note><para>The UML <literal>Element</literal> metaclass from
                  which all other artifacts are derived includes the tagged
                  element <literal>documentation</literal> which is handled by
                  the <emphasis>documentation tab</emphasis> under
                  &argouml;</para></note></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>

    <!-- Association Role properties -->

    <sect2>
      <title>Association Role Properties</title>

      <!-- The properties tool bar -->

      <para>The property tab tool bar for association roles is described in
        <xref linkend="tab.ref.association_role_property_toolbar"/>.</para>

      <table id="tab.ref.association_role_property_toolbar" frame="all"
             colsep="1" rowsep="1" pgwide="1">
        <title>Property tool bar for association role</title>

        <tgroup cols='3' colsep='0' rowsep='0'>
          <colspec align="left"   colwidth="*2"/>
          <colspec align="center" colwidth="*1"/>
          <colspec align="left"   colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Button</entry>
              <entry align="center">Icon</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Go up button -->

            <row>
              <entry>Go up</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_up.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate up through the package
                structure.</para></entry>
            </row>

            <!-- Entry for Go back button -->

            <row>
              <entry>Go back</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_back.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate back to the previous
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Go forward button -->

            <row>
              <entry>Go forward</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_forward.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate forward to the next
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Delete button -->

            <row>
              <entry>Delete</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/property_delete.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>This deletes the association role from the
                model</para>

              <warning><para>This is a deletion from the model
                <emphasis>not</emphasis> just the diagram. To delete an
                association role from the diagram, but keep it within the
                model, use the main menu <literal>Edit/Remove From
                Diagram</literal> (Ctrl-R).</para></warning></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
  
      <!-- Fields in the properties tab -->

      <para>The fields on the property tab for association roles are described
        in <xref linkend="tab.ref.association_role_property_fields"/>.</para>

      <table id="tab.ref.association_role_property_fields" frame="all"
        colsep="1" rowsep="1" pgwide="1">
        <title>Property fields for association role</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Field</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Name -->

            <row>
              <entry>Name</entry>

              <entry><para>Text box. The name of the association role. By
                convention association role names start with a lower case
                letter and use bumpy caps to divide words within the
                name.</para>

              <note><para>&argouml; does not enforce this naming
                convention.</para></note></entry>
            </row>

            <!-- Entry for Stereotype -->

            <row>
              <entry>Stereotype</entry>

              <entry><para>Drop down selector.  Association role should be
                provided by default with the UML standard stereotype from the
                superclass, <literal>Association</literal>
                ((<literal>implicit)</literal>) and the additional Association
                stereotypes for the Software Engineering Process and Business
                Modeling in the UML standard (<literal>communicate</literal>
                and <literal>subscribe</literal>). &argouml; also provides the
                stereotypes, <literal>machine</literal>,
                <literal>organization</literal> and
                <literal>person</literal>.</para>

              <caution><para>&argouml; also provides the stereotype
                <literal>realize</literal> for association roles. This appears
                to be an error, since this stereotype properly belongs to the
                <literal>Abstraction</literal>
                metaclass.</para></caution></entry>
            </row>

            <!-- Entry for NavStereo -->

            <row>
              <entry>Navigate Stereotype</entry>

              <entry><para><guiicon><inlinemediaobject><imageobject><imagedata
                  format="GIF" align="center"
                  fileref="images/icons/navigate_stereotype.gif"/>
                </imageobject></inlinemediaobject></guiicon> icon. If a
                  stereotype has been selected, this will navigate you to the
                  stereotype property panel (see <xref
                    linkend="s1.ref.stereotype_class"/>).</para></entry>
            </row>

            <!-- Entry for Namespace -->

            <row>
              <entry>Namespace</entry>

              <entry><para>Text box. Records the namespace for the
                association role. This is the package hierarchy.</para>

              <para>&Button1; click on the entry will take you to the package
                defining this namespace (or the model for the top level
                namespace).</para></entry>
            </row>

            <!-- Entry for Messages -->

            <row>
              <entry>Messages</entry>

              <entry><para>Text area. Lists the messages that are associated
                with this association role.</para>

              <para>&Button1; click takes you to the selected entry, &button2;
                gives a pop up menu with three entries.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected message.</para></listitem>

                <listitem><para><literal>Move Up</literal>. Only available
                  where there are two or more messages, and the message
                  selected is not at the top. It is moved one up the
                  list.</para></listitem>

                <listitem><para><literal>Move Down</literal>. Only available
                  where there are two or more messages listed, and the message
                  selected is not at the bottom. It is moved one down the
                  list.</para></listitem>
              </itemizedlist></entry>
            </row>

            <!-- Entry for Association End Roles -->

            <row>
              <entry>Association End Roles</entry>

              <entry><para>Text area. Lists the ends of this association
                role. An association role can have any number of ends, but two
                is generally the only useful number (link objects can led to a
                third end on instance level diagrams, but this is not supported
                by &argouml;). For more on association end roles see <xref
                linkend="s1.ref.association_end_role"/>.</para>

              <para>The names are listed, unless the association end role has
                no name, when it is shown as <literal>anon</literal>.</para>

              <warning><para>Association end roles are not implemented in the
                current release of &argouml;. Instead an association end (see
                <xref linkend="s1.ref.association_end_use_case"/>) is
                used. This causes some confusion.</para></warning>

              <para>&Button1; click on an association end role will navigate to
                that end. &Button2; click gives a pop up menu with three
                options.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Where an association
                  end exists, navigate to it.</para></listitem>

                <listitem><para><literal>Move Up</literal>. Only available
                  where there are two or more association end roles, and the
                  association end role selected is not at the top. It is moved
                  one up the list.</para></listitem>

                <listitem><para><literal>Move Down</literal>. Only available
                  where there are two or more association end roles listed, and
                  the association end role selected is not at the bottom. It is
                  moved one down the list.</para></listitem>
              </itemizedlist></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>
  </sect1>

  <!-- Association End Role artifact. A sub-artifact of Association Role. -->

  <sect1 id="s1.ref.association_end_role">
    <title>Association End Role</title>

    <warning><para>Association end roles are not implemented in the current
      release of &argouml;. They are replaced by association ends (see
      <xref linkend="s1.ref.association_end_use_case"/>). This does not lead to
      correct behavior, and the user is advised to avoid them.</para></warning>
  </sect1>

  <!-- Message artifact. A sub-artifact of Association Role. -->

  <sect1>
    <title>Message</title>

    <para>A message is a communication between two instances of an association
      role on a specification level collaboration diagram. It describes an
      action which will generate the stimulus associated with the message. On a
      collaboration diagram a message is associated with an association
      role. In the UML metamodel <literal>Message</literal> is a sub-class of
      <literal>ModelElement</literal>.</para>

    <note><para>On an instance level collaboration diagram, the stimuli between
      instances are shown on the links, rather than the messages that describe
      the communication channel.</para></note>

    <para>The message is represented on a sequence diagram in &argouml; by its
      name separated by a colon from the expression defining the associated
      action, followed by an upward pointing arrow. By convention the name is
      the message sequence number, either as an integer or as a decimal number
      to show hierarchy.</para>

    <note><para>&argouml; will provide a sequence number as the default name
      for a newly created message.</para></note>

    <caution><para>The arrow should show the direction of the communication,
      but in the current release of &argouml; is always shown pointing
      upwards. It should also be stereotyped to indicate whether the associated
      action is call, synchronous send, asynchronous send or return (usually
      just implied). The association role may be constrained as
      <literal>new</literal>, <literal>destroyed</literal> or
      <literal>transient</literal> if it is created, destroyed or both
      respectively during the interaction. However these are not shown as
      messages, since they are actions (create, terminate, destroy) of the
      associated classifier roles, not communications.</para></caution>

    <warning><para>The current release of &argouml; has problems with message
      positioning and will tend to place them on top of each other, refusing to
      accept moved positions as permanent.</para></warning>

    <!-- Message details tabs -->

    <sect2>
      <title>Message Details Tabs</title>

      <para>The details tabs that are active for messages are described in
        <xref linkend="tab.ref.message_details_tabs"/>.</para>

      <table id="tab.ref.message_details_tabs" frame="all" colsep="1"
             rowsep="1" pgwide="1">
        <title>Details tabs for message of all forms</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Tab</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for ToDoItem tab -->

            <row>
              <entry>ToDoItem</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Properties tab -->

            <row>
              <entry>Properties</entry>

              <entry><para>See <xref
                  linkend="tab.ref.message_property_toolbar"/> and <xref
                  linkend="tab.ref.message_property_fields"/>
                below.</para></entry>
            </row>

            <!-- Entry for Documentation tab -->

            <row>
              <entry>Documentation</entry>

              <entry><para>Standard tab.</para></entry>
            </row>

            <!-- Entry for Style tab -->

            <row>
              <entry>Style</entry>

              <entry><para>Standard tab. The values for the bounds of the
                message define the bounding box of the message. The
                <literal>Fill</literal> field defines the fill color of the
                arrow head, and the <literal>Line</literal> field defines the
                arrow line. Altering the <literal>Shadow</literal> field has no
                effect.</para>

              <caution><para>In the current release of &argouml; changing the
                values of the <literal>Bounds</literal> field is possible, but
                will make only a temporary change to the position of the
                message. Selecting any artifact on the screen causes the
                message to return to its original position and the original
                values to be restored. This means messages end up on top of
                each other.</para></caution></entry>
            </row>

            <!-- Entry for Source tab -->

            <row>
              <entry>Source</entry>

              <entry><para>Standard tab, showing the message name and action
                expression separated by a double colon
                (<literal>::</literal>).</para>

              <caution><para>A message probably should not generated any code
                of itself. That should be left to the action and possibly
                stimulus associated with it. In any case changes to this tab
                are ignored.</para></caution></entry>
            </row>

            <!-- Entry for Constraints tab -->

            <row>
              <entry>Constraints</entry>

              <entry><para>Standard tab. There are no standard constraints
                defined for <literal>Message</literal> within the UML
                metamodel.</para></entry>
            </row>

            <!-- Entry for Tagged Values tab -->

            <row>
              <entry>Tagged Values</entry>

              <entry><para>Standard tab. In the UML metamodel,
                <literal>Message</literal> has the following standard
                tagged values defined.</para>

                <itemizedlist>
                  <listitem><para><literal>derived</literal> (from the
                    superclass, <literal>ModelElement</literal>). Values
                    <literal>true</literal>, meaning the message is
                    redundant&mdash;it can be formally derived from other
                    elements, or <literal>false</literal> meaning it
                    cannot.</para>

                  <note><para>Derived messages still have their value in
                    analysis and design to introduce useful names or concepts,
                    and in design to avoid
                    re-computation.</para></note></listitem>
                </itemizedlist>

                <note><para>The UML <literal>Element</literal> metaclass from
                  which all other artifacts are derived includes the tagged
                  element <literal>documentation</literal> which is handled by
                  the <emphasis>documentation tab</emphasis> under
                  &argouml;</para></note></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>

    <!-- Message properties -->

    <sect2>
      <title>Message Properties</title>

      <!-- The properties tool bar -->

      <para>The property tab tool bar for messages is described in <xref
        linkend="tab.ref.message_property_toolbar"/>.</para>

      <table id="tab.ref.message_property_toolbar" frame="all" colsep="1"
             rowsep="1" pgwide="1">
        <title>Property tool bar for message</title>

        <tgroup cols='3' colsep='0' rowsep='0'>
          <colspec align="left"   colwidth="*2"/>
          <colspec align="center" colwidth="*1"/>
          <colspec align="left"   colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Button</entry>
              <entry align="center">Icon</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Go up button -->

            <row>
              <entry>Go up</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_up.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate up through the package
                structure.</para></entry>
            </row>

            <!-- Entry for Go back button -->

            <row>
              <entry>Go back</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_back.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate back to the previous
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Go forward button -->

            <row>
              <entry>Go forward</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/navigate_forward.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>Navigate forward to the next
                artifact.</para>

              <warning><para>This feature is currently broken within &argouml;
                and there is no guarantee where (if anywhere) this button will
                take you.</para></warning></entry>
            </row>

            <!-- Entry for Delete button -->

            <row>
              <entry>Delete</entry>

              <entry><guiicon><inlinemediaobject><imageobject><imagedata
                format="GIF" align="center"
                fileref="images/icons/property_delete.gif"/>
              </imageobject></inlinemediaobject></guiicon></entry>

              <entry><para>This deletes the message from the model</para>

              <warning><para>This is a deletion from the model
                <emphasis>not</emphasis> just the diagram. To delete an
                message from the diagram, but keep it within the model, use
                the main menu <literal>Edit/Remove From Diagram</literal>
                (Ctrl-R).</para></warning></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
  
      <!-- Fields in the properties tab -->

      <para>The fields on the property tab for messages are described in
        <xref linkend="tab.ref.message_property_fields"/>.</para>

      <table id="tab.ref.message_property_fields" frame="all" colsep="1"
             rowsep="1" pgwide="1">
        <title>Property fields for message</title>

        <tgroup cols='2' align='left' colsep='0' rowsep='0'>
          <colspec align="left" colwidth="*3"/>
          <colspec align="left" colwidth="*9"/>
          <thead>
            <row>
              <entry align="left">Field</entry>
              <entry align="left">Meaning</entry>
            </row>
          </thead>

          <tbody valign="top">

            <!-- Entry for Name -->

            <row>
              <entry>Name</entry>

            <entry><para>Text box. The name of a message is usually its
              sequence number, either an integer, or a decimal (allowing
              alternative message hierarchies to be clearly
              described). &argouml; will supply an integer sequence number by
              default.</para></entry>
            </row>

            <!-- Entry for Stereotype -->

            <row>
              <entry>Stereotype</entry>

              <entry><para>Drop down selector. Message has no stereotypes by
                default in the UML standard, but &argouml; provides the
                stereotypes, <literal>machine</literal>,
                <literal>organization</literal> and
                <literal>person</literal>.</para>

              <caution><para>&argouml; also provides the stereotype
                <literal>realize</literal> for messages. This appears to be an
                error, since this stereotype properly belongs to the
                <literal>Abstraction</literal>
                metaclass.</para></caution></entry>
            </row>

            <!-- Entry for NavStereo -->

            <row>
              <entry>Navigate Stereotype</entry>

              <entry><para><guiicon><inlinemediaobject><imageobject><imagedata
                  format="GIF" align="center"
                  fileref="images/icons/navigate_stereotype.gif"/>
                </imageobject></inlinemediaobject></guiicon> icon. If a
                  stereotype has been selected, this will navigate you to the
                  stereotype property panel (see <xref
                    linkend="s1.ref.stereotype_class"/>).</para></entry>
            </row>

            <!-- Entry for Namespace -->

            <row>
              <entry>Namespace</entry>

              <entry><para>Text box. Records the namespace for the
                message. This is the package hierarchy.</para>

              <para>&Button1; click on the entry will take you to the package
                defining this namespace (or the model for the top level
                namespace).</para></entry>
            </row>

            <!-- Entry for Sender -->

            <row>
              <entry>Sender</entry>

              <entry><para>Text box. Identifies the classifier role which sent
                this message.</para>

              <para>&Button1; click takes you to the sender classifier role,
                &button2; gives a pop up menu with one entry.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected sender classifier role.</para></listitem>
              </itemizedlist></entry>
            </row>

            <!-- Entry for Receiver -->

            <row>
              <entry>Receiver</entry>

              <entry><para>Text box. Identifies the classifier role which
                receives this message.</para>

              <para>&Button1; click takes you to the receiver classifier role,
                &button2; gives a pop up menu with one entry.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected receiver classifier role.</para></listitem>
              </itemizedlist></entry>
            </row>

            <!-- Entry for Predecessor -->

            <row>
              <entry>Predecessor</entry>

              <entry><para>Text area. Identifies the messages, the completion
                of whose execution enables this message.</para>

              <para>&Button1; click takes you to the selected message,
                &button2; gives a pop up menu with one entry.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected message.</para>

                <warning><para>This entry is always grayed out in the current
                  release of &argouml;</para></warning></listitem>
              </itemizedlist></entry>
            </row>

            <!-- Entry for Activator -->

            <row>
              <entry>Activator</entry>

              <entry><para>Text area. Identifies the message which invokes the
                behavior that causes the sending of this message.</para>

              <para>&Button1; click takes you to the selected message,
                &button2; gives a pop up menu with one entry.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected message.</para>

                <warning><para>This entry is always grayed out in the current
                  release of &argouml;</para></warning></listitem>
              </itemizedlist></entry>
            </row>

            <!-- Entry for Action -->

            <row>
              <entry>Action</entry>

              <entry><para>Text box. Lists the action (see
                <xref linkend="s1.ref.action_state"/>) this message invokes to
                raise a stimulus.</para>

              <para>&Button1; click takes you to the selected action, &button2;
                gives a pop up menu with three entries.</para>

              <itemizedlist>
                <listitem><para><literal>Open</literal>. Navigate to the
                  selected action.</para></listitem>

                <listitem><para><literal>Delete</literal>. Delete the selected
                  action.</para></listitem>

                <listitem><para><literal>Add</literal>. Add a new
                  action.</para>

                <warning><para>The current release of &argouml; seems to have
                  some problems in this area. Adding actions seems to replace
                  any existing action, and also causes message graphics to
                  become tangled together. Use with considerable
                  care.</para></warning></listitem>
              </itemizedlist></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </sect2>
  </sect1>

</chapter>

<!-- Local stuff for Emacs - please do not delete

Local Variables:
mode: xml
sgml-doctype: "argomanual.xml"
sgml-parent-document: ("argomanual.xml" "chapter" nil)
sgml-validate-command: "nsgmls -wxml -sv ../docbook-setup/xml.dcl \
  argomanual.xml"
End:

-->
