#
# Default Win32 for Argo/UML
#
# Must specify the relative dir of this
# make file to $(TOP).
#
REL_DIR=uci\uml
!INCLUDE Sources.list
!INCLUDE ..\..\NMakefile.config


archive:
	$(COPY) *.class $(OUT_DIR)
!IF ! EXIST( $(OUT_DIR)\templates )
	$(MKDIR) $(OUT_DIR)\templates
!ENDIF
	$(CD) templates
	$(COPY) *.* $(OUT_DIR)\templates
	$(CD) ..

